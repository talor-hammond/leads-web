import {
    GET_POSTS,
    ADD_POST,
    GET_POST_BY_POST_ID,
    GET_POSTS_BY_USER_ID
} from '../actions/general_posts'

export function general(state = [], action) {
    switch (action.type) {

    }
}